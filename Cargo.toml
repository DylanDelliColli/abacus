[workspace]
resolver = "3"
members = [
    "abacus-core",
    "abacus-runtime",
    "abacus-state",
    "abacus-usecase-journeys",
    "abacus-work",
]

# abacus-cli joins the workspace when its phase opens
# (docs/migration.md, Phase 5).

[workspace.package]
version = "0.0.1"
edition = "2024"
