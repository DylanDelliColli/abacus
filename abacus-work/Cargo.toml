[package]
name = "abacus-work"
version.workspace = true
edition.workspace = true

[dependencies]
abacus-core = { path = "../abacus-core" }
# Operator-authorized 2026-08-05 for br provider JSON parsing (omw.2).
# Scope: this crate only; abacus-core stays dependency-free.
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Operator-authorized 2026-08-05 for the bead-content digest (omw.8):
# vetted RustCrypto SHA-256; hand-rolled cryptography is forbidden.
sha2 = "0.10"
